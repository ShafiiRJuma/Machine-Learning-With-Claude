# Scenario 01 — UDSM Registration System

**Setting:** You're building a small script for the University of Dar es Salaam registration desk. When a new student registers, the desk clerk needs to store their basic info and print a clean confirmation slip.

### The raw inputs

```python
student_name = "Juma Mwakisu"
student_age = 21
gpa = 3.6
is_registered = True
```

### What you must build

Write plain Python (no functions yet) that:

1. Stores the four variables above
2. Prints a confirmation message using an f-string, in this exact style:

### Rules

- Must use an f-string (not string concatenation with `+`)
- GPA must show exactly 2 decimal places

---

Give it your best shot — write the code and send it over, and I'll grade it.
